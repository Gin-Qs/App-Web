{
  "name": "ginqs-logistics",
  "version": "0.1.0",
  "private": true,
  "description": "Gin-Qs B2B logistics platform — web app + shared core, ready for future iOS/Android apps.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @ginqs/web",
    "build": "npm run build --workspace @ginqs/web",
    "preview": "npm run preview --workspace @ginqs/web",
    "typecheck": "npm run typecheck --workspace @ginqs/web"
  },
  "engines": {
    "node": ">=20"
  }
}
